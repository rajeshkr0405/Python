# set is the collection of the unordered items.
# Each element in the set must be unique & immutable

# nums = {1,2,3,4}
# set2 = {1,2,2,2}
# #rpeated elements stored only once , so it resolved to {1,2}
# null_set = set() #empty set syntaax
# collection = {1,2,3,4,4,4, "hello" , "world", "hello"} # it will exculde the duplicate value 
# print(collection)
# print(type(collection))
# print(len(collection))
collection = set() # empty set ; syntex
print(type(collection))